<!DOCTYPE html>
<html lang="en">
<head>
  <title>Student Login Portal</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script>
  <link href="https://fonts.googleapis.com/css?family=Ubuntu" rel="stylesheet">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.2/css/all.css" integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr" crossorigin="anonymous">
  
  <style>
    body {
      font-family: 'Ubuntu', sans-serif;
      font-size: 1.5rem; /* Increased base font size */
      height: 100vh;
      margin: 0;
      background-color: #f4f4f4;
    }
    .header {
      background-color: #002147;
      padding: 2rem;
      text-align: center;
      color: white;
      font-size: 2.5rem; /* Increased header font size */
      font-weight: bold;
    }
    .tab-content {
      margin-top: 40px;
    }
    .profile-section {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      padding: 2rem;
      background-color: #f9f9f9;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.2);
      height: auto;
    }
    .profile-image img {
      width: 200px;
      height: 200px;
      border-radius: 50%;
      margin: 20px;
    }
    .student-info {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: start;
      margin-left: 30px;
    }
    .student-info p {
      font-size: 1.5rem; /* Increased text size for student info */
      margin: 12px 0;
      color: #333;
    }
    .tab-pane {
      padding: 30px;
      background-color: #f1f1f1;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    .nav-tabs > li > a {
      font-size: 1.5rem; /* Increased tab font size */
      padding: 18px 35px;
      color: #555;
    }
    .nav-tabs > li.active > a {
      background-color: #002147;
      color: white;
    }
    .activities-container {
      display: flex;
      height: 60vh;
      background-color: #f9f9f9;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.2);
    }
    .activities-menu {
      width: 25%;
      padding: 20px;
      background-color: #f1f1f1;
      border-right: 1px solid #ddd;
      display: flex;
      flex-direction: column;
      gap: 15px;
    }
    .activity-button {
      padding: 15px;
      font-size: 1.5rem;
      text-align: center;
      background-color: #002147;
      color: white;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.2s ease;
    }
    .activity-button:hover {
      background-color: #1a3b6e;
    }
    .activity-content {
      width: 75%;
      padding: 25px;
      overflow-y: auto;
      display: none;
    }
    .activity-content.active {
      display: block;
    }
    .feedback-buttons {
      font-size: 1.5rem;
      padding: 12px;
      margin: 20px;
      background-color: #5388c5;
      color: white;
      border-radius: 8px;
      text-align: center;
      cursor: pointer;
      
    }
    .feedback-buttons:hover {
      background-color: #1a3b6e;
    }
    .complaints-container {
      display: flex;
      justify-content: space-around;
      flex-wrap: wrap;
    }
    .complaint-item {
      width: 200px;
      text-align: center;
      background-color: white;
      padding: 25px;
      border-radius: 10px;
      box-shadow: 0 3px 8px rgba(0,0,0,0.2);
      margin: 20px;
      transition: transform 0.3s;
      font-size: 1.3rem;
    }
    .complaint-item:hover {
      transform: scale(1.1);
      cursor: pointer;
    }
    .pivot-container {
      text-align: center;
      background-color: #e9e9e9;
      padding: 30px;
      border-radius: 10px;
    }
    .btn-leave {
      width: 100%;
      padding: 20px;
      font-size: 1.5rem;
      background-color: #ff6347;
      color: white;
      border-radius: 10px;
      transition: background-color 0.3s;
    }
    .btn-leave:hover {
      background-color: #e5533e;
    }

  </style>
</head>

<body>
  <div class="header">Student Login Portal</div>

  <div class="container">
    <ul class="nav nav-tabs">
      <li class="active"><a data-toggle="tab" href="#profile"><i class="fas fa-user"></i> Profile</a></li>
      <li><a data-toggle="tab" href="#activities"><i class="fas fa-chart-line"></i> My Activities</a></li>
      <li><a data-toggle="tab" href="#complaints"><i class="fas fa-exclamation-circle"></i> Complaints</a></li>
      <li><a data-toggle="tab" href="#pivot"><i class="fas fa-pivot-table"></i> Student Pivot</a></li>
      <li><a href="/"><i class="fas fa-sign-out-alt"></i> Log Out</a></li>
    </ul>

    <div class="tab-content">
      <div id="profile" class="tab-pane fade in active">
        <div class="profile-section">
          <div class="profile-image">
            <img src="<%= student.picture %>" alt="User">
          </div>
          <div class="student-info">
            <p><strong>Name:</strong> <%= student.first_name %> <%= student.last_name %></p>
            <p><strong>ID:</strong> <%= student.username || "-" %></p>
            <p><strong>Email:</strong> <%= student.email || "-" %></p>
            <p><strong>Branch:</strong> <%= student.branch || "-" %></p>
            <p><strong>Father name:</strong> <%= student.father_name || "-" %></p>
            <p><strong>Date of Birth:</strong> <%= student.DOB || "-" %></p>
            <p><strong>Address:</strong> <%= student.address || "-" %></p>
            <p><strong>Contact:</strong> <%= student.contact || "-" %></p>
          </div>
        </div>
      </div>

      <div id="activities" class="tab-pane fade">
        <h3 class="text-center">My Activities</h3>
        <div class="activities-container">
          <!-- Left Column: Buttons -->
          <div class="activities-menu">
            <div class="activity-button" onclick="showActivity('feedback')">Feedback, Grievances & Requests</div>
            <div class="activity-button" onclick="showActivity('notices')">Circular and Notices</div>
            <div class="activity-button" onclick="showActivity('leave')">Leave Application</div> <!-- Leave button -->
          </div>
          
          <!-- Right Column: Activity Content -->
          <div id="feedback" class="activity-content">
            <div class="feedback-buttons" ><a href="/feedback_request/<%= student._id %>" style="color: white;">Submit New</a></div>
            <div class="feedback-buttons" ><a href="/studentFeedbacks/<%= student._id %>" style="color: white;">View My Submissions</a></div>
          </div>
          
          <div id="notices" class="activity-content">
            <h4>Circular and Notices</h4>
              <% if (notices.length > 0) { %>
                <% notices.forEach(function(notice) { %>
                <div class="notice-item">
                    <h5><%= notice.title %></h5>
                    <p><%= notice.description %></p>
                    <p><span class="emergency-level">Emergency Level: </span><%= notice.emergencyLevel %></p>
                </div>
              <% }); %>
        <% } else { %>
            <p>No notices available at the moment.</p>
        <% } %>
            
          </div>
          
          <div id="leave" class="activity-content">
            <div class="feedback-buttons" ><a href="/leave_request/<%= student._id %>" style="color: white;">Apply New Leave</a></div>
            <div class="feedback-buttons" ><a href="/studentLeaves/<%= student._id %>" style="color: white;">My Applications</a></div>
          </div>
        </div>
      </div>

      <div id="complaints" class="tab-pane fade">
        <h3 class="text-center">Complaints</h3>
        <div class="complaints-container">
          <a href="/complaint_request/<%= student._id %>">
          <div class="complaint-item" style="color: black;">
            <i class="fas fa-file-signature fa-2x"></i>
            <p>File a complaint</p>
          </div>
          </a>
          <a href="/studentComplaints/<%= student._id %>">
          <div class="complaint-item" style="color: black;">
            <i class="fas fa-clipboard-list fa-2x"></i>
            <p>My complaints</p>
          </div>
          </a>
          <a href="/lostandFound">
          <div class="complaint-item" style="color: black;">
            <i class="fas fa-search-location fa-2x"></i>
            <p>Lost and Found</p>
          </div>
         </a>
        </div>
      </div>


      <div id="pivot" class="tab-pane fade">
        <div class="pivot-container">
          <h3>Welcome to the Student Pivot Section</h3>
          <p>This section provides data analytics and pivot tables for student activities.</p>
        </div>
      </div>
    </div>
  </div>

  <script>
    function showActivity(activity) {
      const activities = document.querySelectorAll('.activity-content');
      activities.forEach(function(content) {
        content.classList.remove('active');
      });

      const activeActivity = document.getElementById(activity);
      if (activeActivity) {
        activeActivity.classList.add('active');
      }
    }
  </script>

</body>
</html>

